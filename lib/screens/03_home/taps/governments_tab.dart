import 'package:flutter/material.dart';

class GovernmentsTab extends StatelessWidget {
  const GovernmentsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Governments Tab'),
    );
  }
}
