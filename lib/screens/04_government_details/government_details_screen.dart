import 'package:flutter/material.dart';

class GovernmentDetailsScreen extends StatelessWidget {
  const GovernmentDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Government Details Screen'),
      ),
    );
  }
}
